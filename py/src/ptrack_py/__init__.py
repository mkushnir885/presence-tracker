"""
ptrack_py — binary-only modules bundled into the PyInstaller `ptrack_py`
executable. Holds the CLI entry point plus the formats Go shells out for
(CSV reports, GUI stats JSON). Notebook users should depend on
`ptrack_analytics` directly instead.
"""
