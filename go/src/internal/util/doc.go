// Package util holds small, dependency-free helpers shared across the
// project. Anything specific to a single adapter or subsystem belongs
// in its own package; only genuinely reusable building blocks live
// here.
package util
