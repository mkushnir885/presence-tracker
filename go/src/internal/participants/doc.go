// Package participants manages the persistent participant registry that maps
// platform-specific identifiers to stable internal [ParticipantID]s and stores
// the pairing state between platform identities and messenger handles.
package participants
