// Package oauth implements the OAuth 2.0 Authorization Code + PKCE flow
// used by the Meet and Zoom provider adapters. It handles browser-based
// consent, localhost callback capture, token exchange, token refresh, and
// token persistence to a JSON file in the application data directory.
package oauth
