// Package config loads and validates the YAML configuration file and its
// companion secrets file. The parsed [Config] struct is the single source of
// runtime configuration for all other packages.
package config
